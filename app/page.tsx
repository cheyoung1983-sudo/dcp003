'use client';

import React from 'react';
import App from '@/App';

/**
 * Next.js App Router root page.
 * Imports and mounts the main bench application with full tab navigation,
 * real-time telemetry, and persistent state preserved.
 */
export default function Page() {
  return <App />;
}
